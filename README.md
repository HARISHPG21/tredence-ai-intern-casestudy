# tredence-ai-intern-casestudy
Self-pruning neural network trained on CIFAR-10 using learnable sigmoid gates and L1 sparsity regularization. Implements a custom PrunableLinear layer, custom loss function, and training loop. Compares sparsity-accuracy trade-off across three lambda values with gate distribution analysis.
